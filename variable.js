// declare variable with let
// if variable value is changes so that variable is declared with let
let x = 8;
let y = 5;

x = 2;
y = 3;

let sum = x + y;
// console.log(sum);

// declare variable with const
// if variable value isn't changes than declared with const

// for example

const fatherName = "y";
// fatherName = "y";
// const variable value isn't re-asin but is reuseable
console.log(fatherName);
